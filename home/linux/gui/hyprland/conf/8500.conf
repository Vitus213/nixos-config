   # Monitors
# ===================================================
    monitor=DP-1,1920x1080@60,1920x0,1
    monitor=eDP-1,1920x1080@60,0x0,1
    workspace = 1, monitor:DP-1
    workspace = 2, monitor:DP-1
    workspace = 3, monitor:DP-1
    workspace = 4, monitor:DP-1
    workspace = 5, monitor:eDP-1
    workspace = 6, monitor:eDP-1
    workspace = 7, monitor:eDP-1
    workspace = 8, monitor:eDP-1
# ============================================================================
# WINDOW RULES (NEW FORMAT)
# ============================================================================

windowrule = match:class ^(obsidian)$, workspace 1
windowrule = match:class ^(Typora)$, workspace 1
# Workspace 2: Tools & Editors
windowrule = match:class ^(thunder)$, workspace 2
windowrule = match:class ^(code)$, workspace 2
windowrule = match:class ^(cursor)$, workspace 2



# Workspace 3: Browser
windowrule = match:class ^(firefox)$, workspace 3

windowrule = match:class ^(microsoft-edge)$, workspace 3

# Workspace 7: Chat Apps
windowrule = match:class ^(QQ)$, workspace 7
windowrule = match:class ^(wechat)$, workspace 7
windowrule = match:class ^(org.telegram.desktop)$, workspace 7
windowrule = match:class ^(feishu)$, workspace 7


# Workspace 8: System & Network
windowrule = match:class ^(org.wireshark.Wireshark)$, workspace 8
windowrule = match:class ^(clash-verge)$, workspace 8