    # Monitors
# ===================================================
    # Monitor for Host 1
    monitor=DP-2,2560x1440@200,0x0,1,transform,0,bitdepth,10
    monitor=DP-1, disable

    # 为显示器分配工作区 for Host 1
    workspace = r[1-10], monitor:DP-2
    
# ============================================================================
# WINDOW RULES (NEW FORMAT)
# ============================================================================

# Workspace 1: Notes & Writing
windowrule = match:class ^(obsidian)$, workspace 1
windowrule = match:class ^(typora)$, workspace 1

# Workspace 2: Development (Editors)
windowrule = match:class ^(code)$, workspace 2
windowrule = match:class ^(cursor)$, workspace 2

# Workspace 3: Web Browser
windowrule = match:class ^(microsoft-edge)$, workspace 3
windowrule = match:class ^(firefox)$, workspace 3

# Workspace 4: Zed 
windowrule = match:class ^(dev.zed.Zed)$, workspace 4

# Workspace 9: Chat & Social
windowrule = match:class ^(QQ)$, workspace 9
windowrule = match:class ^(wechat)$, workspace 9
windowrule = match:class ^(org.telegram.desktop)$, workspace 9
windowrule = match:class ^(feishu)$, workspace 9

# Workspace 10: Tools & System
windowrule = match:class ^(thunder)$, workspace 10
windowrule = match:class ^(org.wireshark.Wireshark)$, workspace 10
windowrule = match:class ^(clash-verge)$, workspace 10